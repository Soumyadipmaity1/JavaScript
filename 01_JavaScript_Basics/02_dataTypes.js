"use script"; // Treat all JS code as newer ES6 version

console.log(3+4);   // code readabililty should be high and code should be self-explanatory

  let name = 'Soumyadip';
    let age = 30;
    let isApproved = true;

    console.log(typeof name);
    console.log(typeof 30);
    console.log(typeof null); // object 
    console.log(typeof undefined); // undefined